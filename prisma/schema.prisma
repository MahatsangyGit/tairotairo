generator client {
  provider     = "prisma-client"
  output       = "../app/generated/prisma"
  url          = env("DATABASE_URL")
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}
